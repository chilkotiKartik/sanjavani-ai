import { CheckInsView } from '@/components/checkins/checkins-view';

export const metadata = { title: 'Check-ins — Sanjeevani' };

export default function CheckInsPage() {
  return <CheckInsView />;
}
