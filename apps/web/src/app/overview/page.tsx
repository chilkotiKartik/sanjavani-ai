import { OverviewView } from '@/components/overview/overview-view';

export const metadata = {
  title: 'Sanjeevani — speak naturally, get the right next step',
  description:
    'Voice-first medical triage and hospital navigation for India, in Hindi, English and Hinglish, with a deterministic emergency check that runs before any model.',
};

export default function OverviewPage() {
  return <OverviewView />;
}
